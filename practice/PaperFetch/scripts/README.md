# scripts
`utils.py` から `dl_pdf()` 関数を使って, `data.csv` にある論文を `main.py` の実行で `utilizers/practice/PaperFetch/output` の中に保存する.

## 実行手順
1. `main.py` をターミナル上で次のように実行する
    ```sh
    python3 main.py
    ```